#include<stdio.h>
#include<math.h>

int b=2, e=2, tot;


int main(tot)(int b, int e);
{
    if(e==0)
        return 1;
    else
        return(b*pow(b,e-1));
}

// int Power(int base, int exponent){long result = 1;   while (exponent!=0){result *=base;  --exponent;} return result;}