#include <stdio.h>

int computeSum( (1) , (2) )       
{
    int sum = 0;

    for(int i = 0; i < (3); i++)   
    {
        sum = sum + (4);          
    }

    return (5);                    
}

int main()
{
    int arr[5];

    for(int i = 0; i < 5; i++)
    {
        scanf("%d", (6));          
    }

    int result = computeSum( (7) , (8) );

    printf("Sum = %d\n", result);

    return 0;
}